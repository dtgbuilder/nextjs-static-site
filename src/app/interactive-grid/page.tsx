import {Box} from "@mui/material";
import {DashboardWrapper} from "@/component/grid/InteractiveGrid"

export default function Page() {
    return (
        <Box
            sx={{
                backgroundColor: "#0a0a0a"
            }}
        >
            <DashboardWrapper/>
        </Box>
    );
}
